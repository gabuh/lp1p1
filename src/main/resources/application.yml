spring:
  datasource:
    url: jdbc:h2:mem:lp1p1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    open-in-view: true
  h2:
    console:
      enabled: true
      path: /h2-console
